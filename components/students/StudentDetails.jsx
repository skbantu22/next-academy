export default function StudentDetails({ student }) { const metric = (value) => value == null ? "No data yet" : `${value}%`; return <dl className="grid gap-3 text-sm">{[["Student ID",student.studentId],["Name",student.displayName],["Email",student.email],["Phone",student.phone],["Status",student.active === false ? "Inactive" : "Active"],["Enrolled course(s)",student.courseNames],["Progress",metric(student.progress)],["Attendance",metric(student.attendance)]].map(([label,value]) => <div key={label}><dt className="text-xs text-slate-400">{label.toUpperCase()}</dt><dd>{value || "No data yet"}</dd></div>)}</dl>; }
