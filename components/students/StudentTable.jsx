export default function StudentTable({ students, search, onSearch, onView, onEdit, onDeactivate }) {
 const visible = students.filter((item) => `${item.studentId || ""} ${item.displayName || ""} ${item.email || ""} ${item.phone || ""}`.toLowerCase().includes(search.trim().toLowerCase())); const metric = (value) => value == null ? "--" : `${value}%`;
 return <section className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm md:p-6"><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div><h3 className="font-bold">Student directory</h3><p className="mt-1 text-xs text-slate-500">Search by ID, name, email, or phone.</p></div><input value={search} onChange={(e) => onSearch(e.target.value)} placeholder="Search students" className="w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-sm sm:w-64" /></div>{visible.length ? <div className="overflow-x-auto"><table className="w-full min-w-[1000px] text-left text-sm"><thead className="border-b text-[10px] uppercase tracking-wider text-slate-400"><tr>{["ID","Name","Course enrolled","Progress","Status","Attendance","Phone","Actions"].map((label) => <th key={label} className="p-3">{label}</th>)}</tr></thead><tbody>{visible.map((item) => <tr key={item.id} className="border-b border-slate-100"><td className="p-3 font-semibold">{item.studentId || "--"}</td><td className="p-3"><b>{item.displayName || "Unnamed student"}</b><p className="text-xs text-slate-500">{item.email || "No email"}</p></td><td className="p-3">{item.courseNames || "No enrollment"}</td><td className="p-3">{metric(item.progress)}</td><td className="p-3">{item.active === false ? "Inactive" : "Active"}</td><td className="p-3">{metric(item.attendance)}</td><td className="p-3">{item.phone || "--"}</td><td className="space-x-3 p-3 text-xs font-bold"><button onClick={() => onView(item)}>View</button><button className="text-red-600" onClick={() => onEdit(item)}>Edit</button>{item.active !== false && <button className="text-red-700" onClick={() => onDeactivate(item)}>Deactivate</button>}</td></tr>)}</tbody></table></div> : <p className="py-10 text-center text-sm text-slate-500">{search ? "No students match your search." : "No students have been created yet."}</p>}</section>;
}
