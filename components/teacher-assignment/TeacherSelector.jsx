export default function TeacherSelector({ teachers, selectedIds, onChange }) { return <div className="grid gap-2">{teachers.length ? teachers.map((teacher) => <label key={teacher.id} className="flex items-center gap-3 rounded-xl border border-slate-200 p-3 text-sm"><input type="checkbox" checked={selectedIds.includes(teacher.id)} onChange={(event) => onChange(event.target.checked ? [...selectedIds, teacher.id] : selectedIds.filter((id) => id !== teacher.id))}/><span><b>{teacher.displayName || "Unnamed teacher"}</b><small className="ml-2 text-slate-500">{teacher.email}</small></span></label>) : <p className="text-sm text-slate-500">No active teachers are available.</p>}</div>; }
