// eslint-disable-next-line @next/next/no-img-element
function Avatar({ teacher }) { return teacher.photoURL ? <img src={teacher.photoURL} alt={teacher.displayName || "Teacher"} className="h-9 w-9 shrink-0 rounded-full object-cover" /> : <span className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-active text-xs font-bold text-primary">{(teacher.displayName || teacher.email || "?").slice(0, 2).toUpperCase()}</span>; }

export default function TeacherSelector({ teachers, selectedIds, onChange }) { return <div className="grid gap-2">{teachers.length ? teachers.map((teacher) => <label key={teacher.id} className="flex items-center gap-3 rounded-xl border border-border-subtle p-3 text-sm"><input type="checkbox" checked={selectedIds.includes(teacher.id)} onChange={(event) => onChange(event.target.checked ? [...selectedIds, teacher.id] : selectedIds.filter((id) => id !== teacher.id))}/><Avatar teacher={teacher} /><span className="min-w-0"><b className="block truncate">{teacher.displayName || "Unnamed teacher"}</b><small className="block truncate text-muted">{teacher.email}{teacher.userId ? ` · ${teacher.userId}` : ""}</small></span></label>) : <p className="text-sm text-muted">No active teachers are available.</p>}</div>; }
