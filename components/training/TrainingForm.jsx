const statuses = ["Draft", "Upcoming", "Active", "Completed"];
const loadedTeacherQuery = true;
export default function TrainingForm({ course, teachers, teacherQuery = loadedTeacherQuery, saving, onCancel, onSubmit }) { const set = (key) => (event) => course.setForm({ ...course.form, [key]: event.target.value }); const toggle = (id) => course.setForm({ ...course.form, teacherIds: course.form.teacherIds.includes(id) ? course.form.teacherIds.filter((value) => value !== id) : [...course.form.teacherIds, id] }); return <form onSubmit={onSubmit} className="space-y-4"><label className="grid gap-1 text-xs font-bold text-slate-600">Course title<input value={course.form.title} onChange={set("title")} maxLength="160" required className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm" /></label><label className="grid gap-1 text-xs font-bold text-slate-600">Description<textarea value={course.form.description} onChange={set("description")} maxLength="5000" rows="4" className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm" /></label><label className="grid gap-1 text-xs font-bold text-slate-600">Status<select value={course.form.status} onChange={set("status")} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm">{statuses.map((item) => <option key={item}>{item}</option>)}</select></label><fieldset><legend className="text-xs font-bold text-slate-600">Teachers</legend>{teachers.length ? <div className="mt-2 grid gap-2">{teachers.map((teacher) => <label key={teacher.id} className="flex items-center gap-2 rounded-lg bg-slate-50 p-2 text-sm"><input type="checkbox" checked={course.form.teacherIds.includes(teacher.id)} onChange={() => toggle(teacher.id)} />{teacher.displayName || teacher.email || teacher.id}<span className="ml-auto text-xs text-slate-400">{teacher.email}</span></label>)}</div> : <p className="mt-2 text-sm text-slate-500">{teacherQuery ? "No teachers available." : "Loading teachers..."}</p>}</fieldset><div className="flex justify-end gap-3"><button type="button" onClick={onCancel} disabled={saving} className="px-4 py-2 text-sm font-bold text-slate-600">Cancel</button><button disabled={saving} className="rounded-xl bg-red-600 px-4 py-2 text-sm font-bold text-white">{saving ? "Creating course..." : course.editing ? "Save changes" : "Create course"}</button></div></form>; }
